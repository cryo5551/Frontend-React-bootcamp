import { createSlice } from '@reduxjs/toolkit';


const toggler = createSlice({
    name: 'default',
    initialState:{

    },

    reducers: {
        setValue: (initialState, action) => {}
    }
});